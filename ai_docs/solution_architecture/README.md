# solution_architecture — Core Architecture

The core solution architecture referenced during artifact builds: the design,
structure, and conventions the build stage must conform to.

- One topic per file (progressive disclosure — read the summary, load the body when relevant).
- This is the authoritative context a build reads before writing code.
- Payload, not mechanism — knowledge referenced by prompts and stages, never control flow.
